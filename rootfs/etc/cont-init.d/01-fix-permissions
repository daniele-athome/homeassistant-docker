#!/usr/bin/with-contenv bashio

# Create our user (not stricly needed, but I like it)
# FIXME check for existing user instead of voiding the error
adduser -DH -u "$PUID" -g "$PGID" homeassistant homeassistant || true

# Give permission to install packages
chown -R "$PUID:$PGID" /usr/local/bin /usr/local/lib/python*
